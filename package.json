{
  "name": "wasm-clustering",
  "version": "0.2.0",
  "description": "Rust code for clustering locations, compiles to WASM.",
  "main": "dist/wasm.js",
  "module": "dist/wasm.js",
  "typings": "dist/wasm.d.ts",
  "files": [
    "dist/wasm_bg.js",
    "dist/wasm_bg.wasm",
    "dist/wasm_bg.wasm.d.ts",
    "dist/wasm.d.ts",
    "dist/wasm.js"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "wasm-pack build --release --target bundler --out-dir dist && rm dist/.gitignore"
  },
  "author": {
    "name": "Max Franke",
    "email": "Max.Franke@vis.uni-stuttgart.de"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mfranke93/wasm-clustering"
  },
  "license": "MIT",
  "devDependencies": {
    "wasm-pack": "^0.10.0"
  }
}
